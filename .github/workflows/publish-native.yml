name: Publish native build

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  native:
    permissions:
      actions: read
      contents: read
      id-token: write
    uses: MyWallpapers/native-addon-toolchain/.github/workflows/native-addon-build.yml@021ed358b0d242831ea595b4de512b8a50dc684a
