name: Publish native build

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  native:
    permissions:
      actions: read
      contents: read
      id-token: write
    uses: MyWallpapers/native-addon-toolchain/.github/workflows/native-addon-build.yml@f50c749022a48025170541066d2fb37a951360e0
