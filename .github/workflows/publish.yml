name: Publish MyWallpaper release through admission-v1

on:
  push:
    tags: ['v*']

permissions:
  contents: read

jobs:
  release:
    if: github.event.repository.private == false
    concurrency:
      group: mywallpaper-release-${{ github.repository_id }}-${{ github.ref }}
      cancel-in-progress: false
    permissions:
      actions: read
      artifact-metadata: write
      attestations: write
      contents: write
      id-token: write
    # The public promotion branch is discovery-only. Execution is pinned to the
    # exact commit that MyWallpaper independently allowlists server-side.
    uses: MyWallpapers/native-addon-toolchain/.github/workflows/native-addon-build.yml@6aabdf14607a1e3ac6b08a05d84c6ef29f2fb918
    with:
      channel: development
